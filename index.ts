// let userName: string = "Ivan";

// userName.isInteger();
//
// userName = "fgdfg";

// '', "", ``
// 10, 0.5, 0.001, -50, 4e10
// true, false

const isBirthday: boolean = true;
let age: number;
const userName: string = "John";

age = 40;

if (isBirthday) {
  console.log(`Congrats ${userName.toLocaleUpperCase()}, age: ${age + 1}`);
}
